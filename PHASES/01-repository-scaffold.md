# Phase 01 — Python Repository Scaffold

## Goal

Create the greenfield Python application structure and test harness.

## Tasks

- Create `pyproject.toml` and uv lock strategy.
- Create `src/character_model_studio/` modular structure from the architecture spec.
- Add ruff/typecheck/pytest/pytest-qt configuration.
- Add app bootstrap and empty main window.
- Add local paths/logging bootstrap.
- Add SQLite schema initialization.
- Add scripts/commands required by `HARNESS.md`.
- Do not create API/server folders.

## Acceptance criteria

- App opens locally.
- Tests/lint/typecheck execute.
- No C#/JS/QML/FastAPI/Redis/Celery/PostgreSQL architecture exists.

## Completion evidence

Record commands run, tests passed, manual checks performed, and any environment blockers. A phase is not complete from code generation alone.
